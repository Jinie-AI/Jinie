# These must be environment variables in your shell; no secrets are bundled.
# Optional, only after firebase login:
# JINIE_FIREBASE_PROJECT=your-firebase-project-id
# JINIE_DATA_DIR=/path/to/local/project-state
